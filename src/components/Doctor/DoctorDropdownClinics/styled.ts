import { Dropdown } from "@components/Dropdown"
import styled from "styled-components"

export const DoctorDropdownWrapper = styled(Dropdown)`
	padding: 7px 0;
	font-size: 18px;
	line-height: 26px;
	border-radius: 8px;
	border: 1px solid var(--gray-dark-color);
	height: fit-content;
`

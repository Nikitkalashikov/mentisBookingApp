import styled from "styled-components"

export const TagWrapp = styled.p`
	display: flex;
	flex-direction: row;
	align-items: center;
	padding: 4px 8px;
	font-size: 14px;
	line-height: 22px;
	color: var(--text-color);
	background-color: var(--white-color);
	border-radius: 16px;

	svg {
		margin-right: 4px;
		width: 18px;
		height: 18px;
	}
`

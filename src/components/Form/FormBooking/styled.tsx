import styled from "styled-components"

export const FormBookingWrapper = styled.form`
	display: flex;
	flex-direction: column;
`

export const FormBookClose = styled.div`
	cursor: pointer;
	width: 24px;
	height: 24px;
	margin-left: auto;
	margin-right: 0;

	svg {
		width: 100%;
		height: 100%;
		object-fit: contain;
	}
`

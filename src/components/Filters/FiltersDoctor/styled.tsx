import styled from "styled-components"
import { Filters } from "../Filters"

export const FiltersDoctorWrapper = styled(Filters)`
	width: 100%;
`

export const FiltersDoctorList = styled.div`
	display: flex;
	flex-direction: row;
	align-items: center;
`
